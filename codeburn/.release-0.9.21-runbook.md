# 0.9.21 publish runbook (2026-08-24)

Everything below `PREPARED` is done and verified; `YOU` marks the steps only Resham runs; `ME` marks steps Claude executes on your word after the dependency lands.

## PREPARED (done)
- main @ 195bb912: versions 0.9.21 (CLI + app + windows x3), fresh pricing snapshots, CHANGELOG stamped `0.9.21 - 2026-08-24`, README links on 0.9.21. CI green (CI + Tests; Windows Menubar CI was still running — check).
- Full gates: CLI 3664 green, tsc clean, mac menubar 207 green, upgrade harness (earlier), real-data verification GO.
- Desktop DMGs building locally with the 0.9.21 CLI staged inside (arm64, then x64; Linux after).

## Publish order

### 1. npm (CLI) — YOU
```
cd ~/Projects/codeburn/codeburn
npm publish
```
prepublishOnly rebuilds; snapshots are committed so the tree stays clean. Verify: `npm view codeburn version` → 0.9.21.

### 2. CLI tag — YOU
```
git tag v0.9.21 && git push origin v0.9.21
```

### 3. Homebrew tap — ME (after step 1)
Update getagentseal/homebrew-codeburn Formula/codeburn.rb: url to codeburn-0.9.21.tgz + sha256 of the published tarball.
Verify: `brew update && brew upgrade codeburn && codeburn --version`.

### 4. macOS menubar tag — YOU
```
git tag mac-v0.9.21 && git push origin mac-v0.9.21
```
release-menubar.yml builds and publishes the zip (includes today's SwiftPM resource-bundle packaging fix).

### 5. Windows tray tag — YOU
```
git tag windows-v0.9.21 && git push origin windows-v0.9.21
```
release-menubar-windows.yml builds the unsigned MSI release; `codeburn menubar` on Windows then resolves windows-v0.9.21.

### 6. Desktop tag + release — YOU tag, ME assets
```
git tag desktop-v0.9.21 && git push origin desktop-v0.9.21
```
build-windows-installer.yml runs off this tag (Windows desktop installer). I create the GitHub release on the tag and upload the locally built DMGs (+ Linux artifacts as built). README download links start resolving at that moment.

### 7. Windows Store — YOU upload, ME package
Run the `build-windows-store` workflow (workflow_dispatch on main) → produces the appx/msix package → download artifact → upload in Partner Center. I trigger the workflow and hand you the artifact link.

### 8. Website — user merges PR
`release/0.9.21-site` PR on codeburn-app (agent-prepared: versions, changelog page entry, Today-default copy). Deploys via the repo's own runner on merge.

## Post-publish watch — ME
- Menubar canary + wild telemetry for the first hours; verify update banners offer 0.9.21 on all surfaces; verify the desktop app's period totals now match the menubar (the 0.9.20-engine mismatch documented today disappears when the update lands).

## Known open items (not blockers)
- #904 lock-suite CI flake (5 hits today) — first post-release fix.
- #1134 pricing-test re-tightening when LiteLLM syncs today's OpenAI/DeepSeek repricings.
- #1132 preserved-slice repricing enhancement; #1062 WSL (0.9.22); #1013 flatpak (VPS build pending).
